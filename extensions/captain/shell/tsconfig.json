{
	"$comment": "FC/IS enforcement — shell is the coordinator: references core, infra, gates, steps.",
	"extends": "../../../tsconfig.base.json",
	"compilerOptions": { "composite": true, "outDir": ".tsbuild" },
	"include": ["./**/*.ts"],
	"references": [
		{ "path": "../core" },
		{ "path": "../infra" },
		{ "path": "../gates" },
		{ "path": "../steps" },
		{ "path": "../composition" },
		{ "path": "../transforms" }
	]
}
