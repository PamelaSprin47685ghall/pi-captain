{
	"$comment": "FC/IS enforcement — infra may reference core only, NOT shell/.",
	"extends": "../../../tsconfig.base.json",
	"compilerOptions": {
		"composite": true,
		"outDir": ".tsbuild"
	},
	"include": ["./**/*.ts"],
	"references": [{ "path": "../core" }]
}
